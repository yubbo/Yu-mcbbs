// app/(adminpanel)/admin/settings/ui/theme/config/page.tsx — 主题设置
// 针对当前激活主题的专属配置（颜色、字体、布局等）

import { ThemeConfig } from "@/modules/adminpanel/settings/ui/theme/ThemeConfig"

export default function ThemeConfigPage() {
  return <ThemeConfig />
}
